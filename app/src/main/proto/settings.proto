syntax = "proto3";

option java_package = "com.jayden.bluetoothalerts.proto";
option java_multiple_files = true;

enum MonitorMode {
  DEFAULT = 0;
  PASSIVE = 1;
  ALWAYS = 2;
}

message Settings {
  MonitorMode monitor_mode = 1;
  bool foreground_service_enabled = 2;
}